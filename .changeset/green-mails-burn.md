---
"@quentinroy/word-cloud": minor
---

Rename word-cloud event payload property `entry` to `handle`.

This affects `WordAddEvent`, `WordValueChangeEvent`, `WordCheckedChangeEvent`,
and `WordDeleteEvent`.
