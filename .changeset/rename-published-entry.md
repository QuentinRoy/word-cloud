---
"@quentinroy/word-cloud": patch
---

Publish the library entry as `word-cloud.js`/`word-cloud.d.ts` instead of `index.js`/`index.d.ts`. Imports via the `@quentinroy/word-cloud` package name are unaffected.
