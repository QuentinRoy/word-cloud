---
"@quentinroy/word-cloud": minor
---

No-bundler consumers no longer need to resolve `@quentinroy/custom-element-mixins` via their import map — only `matter-js` remains an external runtime dependency.
