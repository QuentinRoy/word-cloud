---
"@quentinroy/word-cloud": patch
---

Improve performance with large word clouds, and let settled clouds go idle to lower CPU usage when nothing is moving. Word spacing and the `wordRepulsion` / `edgeRepulsion` / `inputRepulsion` options are unchanged.
